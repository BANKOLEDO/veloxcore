{
  "name": "veloxcore",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up"
  }
}
